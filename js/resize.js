$('body').flowtype({
    // minimum : 500,
    // maximum : 1200,
    // minFont   : 12,
    // maxFont   : 40,
    // fontRatio : 20
}),
$('container').flowtype({
    minFont: 24,
    maxFont: 36,
    fontRatio: 20
});
