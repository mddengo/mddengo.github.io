$('body').flowtype({
    // minimum : 500,
    // maximum : 1200,
    // minFont   : 12,
    // maxFont   : 40,
    // fontRatio : 20
}),
$('h1').flowtype({
    minFont: 20,
    maxFont: 48,
    fontRatio: 10
}),
$('p').flowtype({
    minFont: 7,
    maxFont: 16,
});
